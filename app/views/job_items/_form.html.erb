<%= form_for(@job_item) do |f| %>
  <% if @job_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_item.errors.count, "error") %> prohibited this job_item from being saved:</h2>

      <ul>
      <% @job_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
